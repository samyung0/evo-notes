Single-line input. Add a leading `icon` for search / filter fields.

```jsx
<Input icon="search" placeholder="Search workspaces…" />
<Input placeholder="Course name" />
```

`size="sm"` for compact toolbars; style the wrapper via `style`, the field via `inputStyle`.

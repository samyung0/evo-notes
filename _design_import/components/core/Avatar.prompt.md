Circular user avatar — shows the image, or tinted initials when no `src`.

```jsx
<Avatar name="Kate Malone" src="/photo.jpg" />
<Avatar name="Kate Malone" size="sm" />
```

Sizes: `xs` (24), `sm` (30), `md` (38), `lg` (48), or any pixel number.

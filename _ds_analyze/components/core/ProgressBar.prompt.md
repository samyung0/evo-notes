Slim bar for course progress and quiz accuracy — color signals health (`green` good, `amber` warning, `coral` weak).

```jsx
<ProgressBar value={62} tone="green" />
<ProgressBar value={41} tone="coral" showLabel />
```

`height` adjusts thickness; `showLabel` appends a % readout.
